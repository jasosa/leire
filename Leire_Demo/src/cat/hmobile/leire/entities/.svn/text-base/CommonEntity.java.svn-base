package cat.hmobile.leire.entities;

public abstract class CommonEntity {
	
	protected boolean checkEquals(Object obj1, Object obj2)
	{
		boolean result;
		if (obj1 == null)
		{
			result = (obj2 == null);
		}
		else
		{
			result = (obj1.equals(obj2));
		}
		return result;
	}
}
